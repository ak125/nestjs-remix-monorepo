name: 🗺️ Generate Sitemaps (Nightly)

on:
  # Déclenchement manuel
  workflow_dispatch:
    inputs:
      type:
        description: 'Sitemap type to generate'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - products
          - blog
          - pages
          - delta
      
      includeHreflang:
        description: 'Include hreflang links'
        required: false
        default: true
        type: boolean
      
      includeImages:
        description: 'Include image sitemaps'
        required: false
        default: true
        type: boolean

  # Déclenchement automatique (3h du matin UTC)
  schedule:
    - cron: '0 3 * * *'

  # Déclenchement sur push tags (releases)
  push:
    tags:
      - 'v*.*.*'

env:
  NODE_VERSION: '20.x'

jobs:
  generate-sitemaps:
    name: Generate & Deploy Sitemaps
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      ##########################################################################
      # SETUP
      ##########################################################################
      
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📚 Install dependencies
        run: npm ci
      
      ##########################################################################
      # BUILD
      ##########################################################################
      
      - name: 🏗️ Build backend
        run: npm run build --workspace=backend
        env:
          NODE_ENV: production
      
      ##########################################################################
      # START SERVICES
      ##########################################################################

      - name: 🐳 Start Redis
        run: |
          docker run -d --name redis-ci -p 6379:6379 redis:7-alpine
          sleep 5
          docker exec redis-ci redis-cli ping

      - name: 📂 Create output directories
        run: |
          mkdir -p public/sitemaps
          mkdir -p logs/cron

      - name: 🚀 Start NestJS backend
        run: |
          cd backend
          npm run start:prod &
          sleep 30  # Wait for backend to be ready
          curl --retry 10 --retry-delay 5 http://localhost:3000/health
        env:
          NODE_ENV: production
          REDIS_URL: redis://localhost:6379
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      
      ##########################################################################
      # GENERATE SITEMAPS
      ##########################################################################
      
      - name: 🔄 Generate Delta Sitemap
        run: |
          curl -X POST "http://localhost:3000/sitemap-v2/delta/generate" \
            -H "Content-Type: application/json" \
            -o delta-response.json
          cat delta-response.json
          
          if ! jq -e '.success' delta-response.json > /dev/null; then
            echo "❌ Delta sitemap generation failed"
            exit 1
          fi
      
      - name: 🗜️ Generate Streaming Sitemaps
        run: |
          TYPE="${{ github.event.inputs.type || 'all' }}"
          HREFLANG="${{ github.event.inputs.includeHreflang || 'true' }}"
          IMAGES="${{ github.event.inputs.includeImages || 'true' }}"

          curl -X POST "http://localhost:3000/sitemap-v2/streaming/generate?type=$TYPE&includeHreflang=$HREFLANG&includeImages=$IMAGES" \
            -H "Content-Type: application/json" \
            -o streaming-response.json
          cat streaming-response.json

          if ! jq -e '.success' streaming-response.json > /dev/null; then
            echo "❌ Streaming sitemap generation failed"
            exit 1
          fi

      - name: 🗺️ Generate All Sitemaps (Unified SEO 2026)
        run: |
          echo "🚀 Generating all sitemaps with unified service..."
          curl -X POST "http://localhost:3000/api/sitemap/generate-all?outputDir=public/sitemaps" \
            -H "Content-Type: application/json" \
            -o unified-response.json
          cat unified-response.json

          if ! jq -e '.success' unified-response.json > /dev/null; then
            echo "❌ Unified sitemap generation failed"
            exit 1
          fi

          echo "✅ Generated sitemaps:"
          jq -r '.data.files[] | "  - \(.name): \(.urlCount) URLs"' unified-response.json || echo "No files array"
          echo ""
          echo "📊 Total: $(jq -r '.data.totalUrls' unified-response.json) URLs in $(jq -r '.data.duration' unified-response.json)ms"
      
      - name: 📊 Fetch Generation Stats
        run: |
          echo "## 📈 Delta Stats"
          curl -s "http://localhost:3000/sitemap-v2/delta/stats" | jq .
          
          echo "## 📦 Streaming Files"
          curl -s "http://localhost:3000/sitemap-v2/streaming/files" | jq '.data | length'
      
      ##########################################################################
      # VALIDATE
      ##########################################################################
      
      - name: ✅ Validate Sitemaps XML
        run: |
          cd public/sitemaps
          
          for file in *.xml; do
            if [ -f "$file" ]; then
              echo "Validating $file..."
              xmllint --noout "$file" || exit 1
            fi
          done
          
          echo "✅ All XML files are valid"
      
      - name: 🔍 Check Sitemap Sizes
        run: |
          cd public/sitemaps
          
          echo "## File Sizes"
          du -sh *
          
          echo "## Compression Ratios"
          for gz in *.xml.gz; do
            if [ -f "$gz" ]; then
              ORIGINAL=$(gzip -l "$gz" | tail -1 | awk '{print $2}')
              COMPRESSED=$(stat -f%z "$gz" 2>/dev/null || stat -c%s "$gz")
              RATIO=$(echo "scale=1; 100 - ($COMPRESSED * 100 / $ORIGINAL)" | bc)
              echo "$gz: ${RATIO}% compression"
            fi
          done
      
      ##########################################################################
      # UPLOAD TO S3 (optionnel)
      ##########################################################################
      
      - name: ☁️ Upload to S3
        if: github.ref == 'refs/heads/main'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-west-1
      
      - name: 📤 Sync sitemaps to S3
        if: github.ref == 'refs/heads/main'
        run: |
          aws s3 sync public/sitemaps/ s3://automecanik-sitemaps/ \
            --delete \
            --cache-control "public, max-age=86400" \
            --metadata-directive REPLACE
          
          echo "✅ Sitemaps uploaded to S3"
      
      ##########################################################################
      # NOTIFY
      ##########################################################################
      
      - name: 💬 Notify Slack
        if: always()
        continue-on-error: true
        uses: slackapi/slack-github-action@v2
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            {
              "text": "${{ job.status == 'success' && '✅' || '❌' }} Sitemap Generation: ${{ job.status }}",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*Sitemap Generation*\nStatus: ${{ job.status }}\nBranch: ${{ github.ref_name }}\nCommit: ${{ github.sha }}"
                  }
                }
              ]
            }
      
      ##########################################################################
      # CLEANUP
      ##########################################################################
      
      - name: 🧹 Cleanup old deltas
        run: |
          curl -X POST "http://localhost:3000/sitemap-v2/delta/cleanup" \
            -H "Content-Type: application/json"
      
      - name: 🐳 Stop Docker services
        if: always()
        run: docker stop redis-ci || true
      
      ##########################################################################
      # ARTIFACTS
      ##########################################################################
      
      - name: 📦 Upload artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: sitemaps-${{ github.run_number }}
          path: |
            public/sitemaps/**
            logs/cron/**
            delta-response.json
            streaming-response.json
            unified-response.json
          retention-days: 7
