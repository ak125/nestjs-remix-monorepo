# ⚠️  NE JAMAIS COMMITER LE FICHIER .env RÉEL
# Copier ce fichier en .env et remplir avec vos vraies valeurs

# === DATABASE ===
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-key

# === REDIS (Cache & Sessions) ===
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=optional-redis-password

# === SESSION ===
SESSION_SECRET=generate-with-openssl-rand-base64-32

# === SECURITY ===
CORS_ORIGIN=http://localhost:3000
NODE_ENV=development  # development | test | production

# === STOCK MANAGEMENT ===
# UNLIMITED = Flux tendu (stock illimité, recommandé pour démarrer)
# TRACKED = Suivi du stock réel avec alertes de réapprovisionnement
STOCK_MODE=UNLIMITED

# === RPC UNIFIÉE V2 (Performance Supabase) ===
# true = Active la RPC V2 unifiée (1 appel au lieu de ~33)
USE_UNIFIED_RPC=false
# true = Appelle get_oem_refs_for_vehicle séparément si la V2 timeout sur les OEM
# Utile si les index OEM ne suffisent pas sur plan Supabase Small
USE_SEPARATE_OEM_RPC=false

# === PAYMENT GATEWAY (Cyberplus/SystemPay BNP Paribas) ===
# Identifiant du site marchand
CYBERPLUS_SITE_ID=your-site-id
# Certificat de production (clé secrète)
CYBERPLUS_CERTIFICAT=your-production-certificate
# Mode: TEST | PRODUCTION
CYBERPLUS_MODE=TEST
# URL de paiement
CYBERPLUS_PAYMENT_URL=https://secure.systempay.fr/vads-payment/
# URL de base de l'application (pour les callbacks)
APP_URL=http://localhost:3000
# URL de base pour les redirections de paiement (utilisé par SystemPayRedirectController)
BASE_URL=http://localhost:3000

# === TEST & DEVELOPMENT ONLY - NE JAMAIS UTILISER EN PRODUCTION ===
# Credentials pour le endpoint /auth/debug-users (dev/test uniquement)
TEST_USER_EMAIL=admin@fafa.fr
TEST_USER_PASSWORD=Test123!_CHANGE_THIS_IN_DEV

# === DEPRECATED - À SUPPRIMER ===
# DEFAULT_ADMIN_PASSWORD=change-this-in-production
