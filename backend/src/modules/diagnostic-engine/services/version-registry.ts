/**
 * VersionRegistry — V1A.0
 *
 * Source unique des versions semver du pipeline Intent Resolution.
 * Bump à chaque changement de classifier rules / recommender sequences / invariants.
 *
 * V1A.0 : `pipeline_version` only.
 * V1.1 : triplet versions (policy_version + reason_taxonomy_version) ajouté.
 */
export const PIPELINE_VERSION = 'v1.0.0' as const;
