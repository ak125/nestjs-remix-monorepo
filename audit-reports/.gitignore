# Raw tool outputs are regeneratable via `npm run audit:all`.
# Only the curated baseline.md is versioned.
phase0-knip.txt
phase0-madge.txt
phase0-depcruise.txt
phase0-astgrep.txt

# Generated dependency graph — regenerate via `npm run audit:graph`
dep-graph.json
dep-graph.dot
dep-graph.svg

# Contract Drift Observatory V1 — ephemeral CI outputs (PR-6)
drift-dashboard.md
contract-health.json
