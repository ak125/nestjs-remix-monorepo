# Placeholder pour satisfaire l'invariant V1-4 du registry overlay
# (ownership.yaml glob `.archive/research/**` doit résoudre à ≥1 file).
#
# Les captures réelles sont sous .archive/research/geo-probe-2026-05-24/raw/<engine>/*.json
# (commitées à la closure du run, séparément du scaffold).
#
# G10 EXPLORATION_BUDGET probes (ADR-081, domain D15).
