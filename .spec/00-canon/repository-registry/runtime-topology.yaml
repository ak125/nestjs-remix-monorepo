# yaml-language-server: $schema=../_schema/runtime-topology.schema.json
# .spec/00-canon/repository-registry/runtime-topology.yaml
#
# AutoMecanik monorepo — canonical runtime contract (PR-5, ADR-058 follow-on).
#
# The `# yaml-language-server:` directive above links this YAML to its generated
# JSON Schema mirror for IDE validation (RedHat YAML extension / Helix LSP / etc.).
# The path is RELATIVE so it resolves correctly in worktrees / sub-checkouts. The
# schema file is gitignored — run `npm run runtime-contract:build` after a fresh
# clone to populate it.
#
# ──────────────────────────────────────────────────────────────────────────────────
#  DOCTRINE — read before adding anything to this file
# ──────────────────────────────────────────────────────────────────────────────────
#  This file holds RUNTIME ENTRYPOINT INVARIANTS ONLY. It is NOT a dumping ground.
#
#  Do NOT add to this file:
#    × queue concurrency / retry policies → use `workers.yaml` (canon roadmap)
#    × cron schedules + repeatable jobs   → use `workers.yaml` or `cron.yaml`
#    × cache TTL / Redis keys             → use `cache.yaml` (canon roadmap)
#    × event subscribers / emitters       → use `events.yaml` (canon roadmap)
#    × HTTP route auth gates / RBAC       → use `capabilities.yaml` (canon roadmap)
#    × RPC signatures + caller surfaces   → use `rpc.yaml` (canon roadmap)
#    × architectural boundaries           → see `architecture.yaml`
#    × DB tables / RLS / access surfaces  → see `db.yaml`
#    × extraction confidence              → it lives in L1 (audit/registry/runtime.json),
#                                           not in this canon. An entrypoint's status
#                                           is not a function of how confidently we
#                                           parsed it.
#
#  If you're tempted to add a section that isn't a runtime entrypoint invariant,
#  open a NEW contract file instead.
#
#  FUTURE-SPLIT WARNING:
#  If a section starts describing INSTANCE-LEVEL config (a specific queue's
#  concurrency, a cron's exact cadence, a cache's specific TTL) instead of
#  INVARIANTS (kind of entrypoint, owner, status, layer), STOP and create a
#  dedicated contract file.
# ──────────────────────────────────────────────────────────────────────────────────
#
# V1 COVERAGE: this contract protects ONE invariant —
#   the **canonical classification of executable runtime entrypoints** with
#   stable ownership, domain attribution, lifecycle status, and layer assignment.
#
# It does NOT cover queue config, cron schedules, cache topology, RPC signatures,
# or HTTP auth gates — those are dedicated contracts (canon roadmap §63-71).
#
# DERIVED-NOT-DECLARED:
#   `layer` IS declared in YAML for human readability + grep-friendliness, but
#   it is MECHANICALLY VALIDATED at build time against:
#     1. `architecture.yaml#layers[].id` (FK existence check) — test §4.5a
#     2. `inferLayerFromPath(path, architectureLayers)` — test §4.5b
#   Any drift (e.g. `path: frontend/... layer: backend`) FAILS the build.
#   The single source of truth is `path`. Future fields derivable from `path`
#   must follow the same pattern or be omitted entirely.
#
# L1 ALIGNMENT:
#   `id` format = `runtime:<path>` (matches Layer 1 audit/registry/runtime.json
#   produced by scripts/registry/build-runtime-registry.js). Cross-contract
#   test §4.2 verifies that every L2 id appears in L1 (subset, not equivalence).
#
# WORKFLOW
#   Edit this YAML → run `npm run runtime-contract:build` → commit ONLY the YAML.
#   The JSON schema mirror at `.spec/00-canon/_schema/runtime-topology.schema.json`
#   is gitignored, regenerated at each build.

schemaVersion: "1.0.0"
adr: ADR-058

entrypoints:
  # V1 SAMPLE — 28 entries from L1 audit/registry/runtime.json (470 total),
  # selected so ALL 9 cross-contract tests pass against the current
  # ownership.yaml + domains.yaml + architecture.yaml + L1 runtime.json.
  #
  # Selection criteria:
  #   - Status LIVE only
  #   - Path covered by an ownership.yaml glob   (test §4.4a)
  #   - Owner present in ownership.yaml          (test §4.4b)
  #   - Domain declared in domains.yaml, ≠ UNKNOWN (test §4.3)
  #   - Layer matches inferLayerFromPath(path)   (test §4.5)
  #
  # Diversification: 8 nestjs-module + 10 nestjs-controller + 10 remix-route.
  #
  # V1 GAPS — intentionally deferred (require ownership.yaml extension first):
  #   - 1 `worker` entry (backend/src/workers/main.ts)
  #   - 5 `other`  entries (backend/src/{main,main.server}.ts;
  #                         frontend/app/{entry.client,entry.server,root}.tsx)
  # These critical entrypoints lack ownership.yaml glob coverage today. Adding
  # them to canon V1 would force ownership.yaml mutations — out of plan §4
  # NO MODIFY scope. See audit-reports/runtime-contract-v1-coverage-2026-05-14.md
  # for the gap analysis and proposed remediation.

  # ── nestjs-module (8 entries — backend boundaries: auth, config, modules) ──
  - id: "runtime:backend/src/auth/auth.module.ts"
    kind: nestjs-module
    path: "backend/src/auth/auth.module.ts"
    layer: backend
    domain: D11
    owner: "@ak125/auth-team"
    status: LIVE
  - id: "runtime:backend/src/config/operating-matrix.module.ts"
    kind: nestjs-module
    path: "backend/src/config/operating-matrix.module.ts"
    layer: backend
    domain: D13
    owner: "@ak125"
    status: LIVE
  - id: "runtime:backend/src/modules/ai-content/ai-content.module.ts"
    kind: nestjs-module
    path: "backend/src/modules/ai-content/ai-content.module.ts"
    layer: backend
    domain: D6
    owner: "@ak125/rag-team"
    status: LIVE
  - id: "runtime:backend/src/modules/cart/cart.module.ts"
    kind: nestjs-module
    path: "backend/src/modules/cart/cart.module.ts"
    layer: backend
    domain: D11
    owner: "@ak125/payments-team"
    status: LIVE
  - id: "runtime:backend/src/modules/diagnostic-engine/diagnostic-engine.module.ts"
    kind: nestjs-module
    path: "backend/src/modules/diagnostic-engine/diagnostic-engine.module.ts"
    layer: backend
    domain: D4
    owner: "@ak125/vehicle-team"
    status: LIVE
  - id: "runtime:backend/src/modules/knowledge-graph/knowledge-graph.module.ts"
    kind: nestjs-module
    path: "backend/src/modules/knowledge-graph/knowledge-graph.module.ts"
    layer: backend
    domain: D7
    owner: "@ak125"
    status: LIVE
  - id: "runtime:backend/src/modules/payments/payments.module.ts"
    kind: nestjs-module
    path: "backend/src/modules/payments/payments.module.ts"
    layer: backend
    domain: D11
    owner: "@ak125/payments-team"
    status: LIVE
  - id: "runtime:backend/src/modules/rag-proxy/rag-proxy.module.ts"
    kind: nestjs-module
    path: "backend/src/modules/rag-proxy/rag-proxy.module.ts"
    layer: backend
    domain: D6
    owner: "@ak125/rag-team"
    status: LIVE

  # ── nestjs-controller (10 entries — HTTP surface: auth, admin, payments, catalog, marketing, seo) ──
  - id: "runtime:backend/src/auth/controllers/auth-login.controller.ts"
    kind: nestjs-controller
    path: "backend/src/auth/controllers/auth-login.controller.ts"
    layer: backend
    domain: D11
    owner: "@ak125/auth-team"
    status: LIVE
  - id: "runtime:backend/src/modules/admin/controllers/admin-gammes-seo-update.controller.ts"
    kind: nestjs-controller
    path: "backend/src/modules/admin/controllers/admin-gammes-seo-update.controller.ts"
    layer: backend
    domain: D8
    owner: "@ak125/admin-team"
    status: LIVE
  - id: "runtime:backend/src/modules/admin/controllers/admin-rag-ingest.controller.ts"
    kind: nestjs-controller
    path: "backend/src/modules/admin/controllers/admin-rag-ingest.controller.ts"
    layer: backend
    domain: D8
    owner: "@ak125/admin-team"
    status: LIVE
  - id: "runtime:backend/src/modules/admin/controllers/seo-cockpit.controller.ts"
    kind: nestjs-controller
    path: "backend/src/modules/admin/controllers/seo-cockpit.controller.ts"
    layer: backend
    domain: D8
    owner: "@ak125/admin-team"
    status: LIVE
  - id: "runtime:backend/src/modules/cart/controllers/cart-analytics.controller.ts"
    kind: nestjs-controller
    path: "backend/src/modules/cart/controllers/cart-analytics.controller.ts"
    layer: backend
    domain: D11
    owner: "@ak125/payments-team"
    status: LIVE
  - id: "runtime:backend/src/modules/catalog/controllers/pieces-clean.controller.ts"
    kind: nestjs-controller
    path: "backend/src/modules/catalog/controllers/pieces-clean.controller.ts"
    layer: backend
    domain: D1
    owner: "@ak125/catalog-team"
    status: LIVE
  - id: "runtime:backend/src/modules/marketing/controllers/marketing-backlinks.controller.ts"
    kind: nestjs-controller
    path: "backend/src/modules/marketing/controllers/marketing-backlinks.controller.ts"
    layer: backend
    domain: D12
    owner: "@ak125/marketing-team"
    status: LIVE
  - id: "runtime:backend/src/modules/payments/controllers/paybox-callback.controller.ts"
    kind: nestjs-controller
    path: "backend/src/modules/payments/controllers/paybox-callback.controller.ts"
    layer: backend
    domain: D11
    owner: "@ak125/payments-team"
    status: LIVE
  - id: "runtime:backend/src/modules/products/cross-selling.controller.ts"
    kind: nestjs-controller
    path: "backend/src/modules/products/cross-selling.controller.ts"
    layer: backend
    domain: D1
    owner: "@ak125/catalog-team"
    status: LIVE
  - id: "runtime:backend/src/modules/seo/controllers/robots-txt.controller.ts"
    kind: nestjs-controller
    path: "backend/src/modules/seo/controllers/robots-txt.controller.ts"
    layer: backend
    domain: D3
    owner: "@ak125/seo-team"
    status: LIVE

  # ── remix-route (10 entries — frontend pages: catch-all, account, admin, blog, commercial) ──
  - id: "runtime:frontend/app/routes/$.tsx"
    kind: remix-route
    path: "frontend/app/routes/$.tsx"
    layer: frontend
    domain: D8
    owner: "@ak125/frontend-team"
    status: LIVE
  - id: "runtime:frontend/app/routes/account.tsx"
    kind: remix-route
    path: "frontend/app/routes/account.tsx"
    layer: frontend
    domain: D8
    owner: "@ak125/frontend-team"
    status: LIVE
  - id: "runtime:frontend/app/routes/admin.invoices.tsx"
    kind: remix-route
    path: "frontend/app/routes/admin.invoices.tsx"
    layer: frontend
    domain: D8
    owner: "@ak125/admin-team"
    status: LIVE
  - id: "runtime:frontend/app/routes/admin.r1-qa.tsx"
    kind: remix-route
    path: "frontend/app/routes/admin.r1-qa.tsx"
    layer: frontend
    domain: D8
    owner: "@ak125/admin-team"
    status: LIVE
  - id: "runtime:frontend/app/routes/admin.seo-hub.content.diagnostics.tsx"
    kind: remix-route
    path: "frontend/app/routes/admin.seo-hub.content.diagnostics.tsx"
    layer: frontend
    domain: D8
    owner: "@ak125/admin-team"
    status: LIVE
  - id: "runtime:frontend/app/routes/admin.system-overview.tsx"
    kind: remix-route
    path: "frontend/app/routes/admin.system-overview.tsx"
    layer: frontend
    domain: D8
    owner: "@ak125/admin-team"
    status: LIVE
  - id: "runtime:frontend/app/routes/blog-pieces-auto.conseils._index.tsx"
    kind: remix-route
    path: "frontend/app/routes/blog-pieces-auto.conseils._index.tsx"
    layer: frontend
    domain: D8
    owner: "@ak125/frontend-team"
    status: LIVE
  - id: "runtime:frontend/app/routes/commercial.shipping.create._index.tsx"
    kind: remix-route
    path: "frontend/app/routes/commercial.shipping.create._index.tsx"
    layer: frontend
    domain: D8
    owner: "@ak125/frontend-team"
    status: LIVE
  - id: "runtime:frontend/app/routes/forgot-password.tsx"
    kind: remix-route
    path: "frontend/app/routes/forgot-password.tsx"
    layer: frontend
    domain: D8
    owner: "@ak125/frontend-team"
    status: LIVE
  - id: "runtime:frontend/app/routes/products.ranges.tsx"
    kind: remix-route
    path: "frontend/app/routes/products.ranges.tsx"
    layer: frontend
    domain: D8
    owner: "@ak125/frontend-team"
    status: LIVE
