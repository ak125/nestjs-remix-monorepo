# Prompt de reprise — session MassDoc, état au 2026-09-16 17:00Z

> À coller tel quel au début d'une nouvelle session, depuis `/opt/automecanik/app`.

## Où en est le chantier

Le DROP TecDoc de `tecdoc_map.source_linkages` a été exécuté le **14/09** par une session Codex ;
la fenêtre d'observation D4 s'est fermée le **16/09 06:43:34Z** et la vérification de fermeture est
**propre** : 7 objets supprimés absents, 12/12 conservés présents, séquence `last_value`
520 960 875, base 130 Go / PG 17.4, 18/18 jobs pg_cron actifs avec **729 exécutions depuis le DROP
et 0 échec**, bucket de sauvegarde intact (441 objets / 17,86 Go). Les preuves d'exécution du DROP
(164 Ko utiles) ne sont **versionnées nulle part** — c'est la première action listée ci-dessous.

Un balayage complet de la production a tourné le 16/09 : 7 sondes, 88 agents, 40 constats bruts,
**31 confirmés après double réfutation adverse**. Rapport : `audit/balayage-live-2026-09-16.md`
(envoyé par mail à l'owner). Le skill qui le produit est `live-evidence-sweep` (PR #1494).

## Règles qui ne se négocient pas

- **`D5 DROP GO NON DONNÉ` / `DROP HOLD`** reste en vigueur pour tout nouveau DROP.
- Aucun `DROP` / `TRUNCATE` / changement DB destructif sans accord **nominatif** de l'owner.
- `pieces_media_img*` et le bucket `rack-images` = **zone interdite absolue**, jamais dans une liste
  de nettoyage.
- Ne jamais conseiller de lever le plafond de dépenses sans (1) l'interrupteur
  image-transformations vérifié OFF dans `edge_logs` et (2) une liste chiffrée des postes facturés.
- Ne jamais redimensionner le disque tant que le plafond est actif.
- Zones STOP (accord nominatif) : paiement, prix/stock runtime, panier/commande, RLS et DB
  destructive, SEO indexé, déploiement PROD (tag `v*`).
- Merge `main` = container PREPROD. **Jamais** dire « déployé en PROD » sur un merge.

## Ce qui est ouvert

| PR | Sujet | État |
|---|---|---|
| #1491 | recherche blog sur `ba_preview` | CI verte, CLEAN — attend le merge |
| #1492 | heuristique orphelins par parenté | CI verte, CLEAN — **à merger avant de réactiver cron** |
| #1494 | skill `live-evidence-sweep` + envoi mail + contrat de correction | CI verte, CLEAN |
| #1495 | articles récents ordonnés sur `ba_update` | CI en cours |
| #1496 | `.maybeSingle()` sur gamme sans article | CI en cours |
| #1464 | plan de DROP | OPEN, auto-merge armé |

## À faire, dans cet ordre

1. **Sauvegarder les preuves du DROP** — 164 Ko dans
   `.claude/worktrees/codex-tecdoc-xl-large-20260914/scripts/db/tecdoc-drop-candidate/`
   (`execution-20260914/`, `t232-execution-20260914/`, `t400-execution-20260914/`), branche jamais
   poussée. Un merge de #1464 peut déclencher un GC qui les efface. **Pas** les 15 Mo de
   `capacity-passive-*/result.json`.
2. **Cron de la machine DEV** — masqué depuis le 14/09 18:17:11, non par décision d'opérateur mais
   par le `postrm` des paquets d'un `do-release-upgrade` Ubuntu 22.04 → 24.04 **abandonné**
   (755 paquets `iU`, verrou dpkg libre, arrêt sur un prompt conffile). Ordre obligatoire :
   merger #1492 → `git pull` sur DEV → `systemctl unmask cron && systemctl enable --now cron`.
   Réactiver avant #1492 tuerait les sessions Claude vivantes. Ne **pas** lancer
   `dpkg --configure -a` autour d'une fenêtre de maintenance PostgreSQL.
3. **Après la mise à jour PG** — rejouer
   `/opt/automecanik/backups/upgrade-pg-2026-09-17/baseline-upgrade.sql` et `diff` contre
   `baseline-avant-upgrade.txt`, plus les contrôles de `audit/balayage-live-2026-09-16.md` §« À
   vérifier après la fenêtre ». Le même dossier contient `plan-reference-avant-upgrade.txt`
   (capturé le 16/09 20:00Z sous PG 17.4) : plan du prédicat chaud
   `pieces_relation_type WHERE rtp_type_id=19052 AND rtp_pg_id=402` → `Index Scan using
   idx_pieces_relation_type_type_id_composite (cost=0.57..1298.88 rows=1210)`, empreinte md5 du
   corps de `get_soft_404_alternatives` = `99b359d0214ae8cf5f277ecd4116fef2`, et baseline
   `pg_stat_statements` = 596 748 appels à 985,6 ms de moyenne. **Limite explicite** : `EXPLAIN`
   sur l'appel ne rend qu'un `Function Scan` et ne voit pas le plan interne d'une fonction
   plpgsql — pour une régression interne, le détecteur est `auto_explain` (armé à 10 s).
4. **Lots d'hygiène DB** (~2 Go) — débloqués par la fermeture de D4, mais **un GO nominatif par
   lot**, et jamais pendant la fenêtre. Détail : `audit/massdoc-db-hygiene-candidates-2026-09-15.md`.
5. **Deux corrections en attente d'arbitrage owner** — `checkR3Exists` (réparer le gate afficherait
   un **second** lien R3 sur 241 guides indexés, la route en émet déjà un inconditionnel) et
   `ba3_ba_id` (le renommage en `ba3_ba2_id` joindrait des identifiants H2 à des identifiants
   d'article : données fausses en silence). Les deux sont documentées dans le rapport.

## Outillage utile

- **Lecture seule sur la base** : `.claude/skills/live-evidence-sweep/scripts/sweep-psql.sh`
  (`--single-transaction` + `set transaction read only` — `PGOPTIONS` ne traverse **pas** le pooler,
  mesuré). Identifiants dans `backend/.env`, jamais affichés.
- **Envoyer un rapport** : `scripts/ops/analysis-report-mail.sh <rapport.md>` après
  `set -a; . backend/.env; set +a`. Aucun MTA sur la machine — voir mémoire
  `reference_mail_from_devbox_gmail_oauth2_no_mta`.
- **Régénérer le registry depuis un worktree** : exige un `node_modules` autonome (racine +
  frontend + backend) — voir mémoire
  `reference_registry_rebuild_needs_self_contained_node_modules_in_worktree`.

## Pièges mesurés, à ne pas repayer

- Une moyenne calculée sur `postgres_logs` mesure **la queue** : `auto_explain.log_min_duration`
  = 10 s. Erreur constatée ×22. Passer par `pg_stat_statements` ou `edge_logs.origin_time`.
- Le jeton de management Supabase (`sbp_`) est **révoqué** : 401 partout. D3 (sauvegardes/PITR) n'est
  prouvable que dans le tableau de bord.
- L'API de journaux plafonne à **24 h** — pour 48 h, croiser avec `cron.job_run_details`.
- Le connecteur Gmail de claude.ai exige une autorisation interactive : inutilisable en session
  non interactive.
